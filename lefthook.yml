pre-commit:
  parallel: true
  commands:
    lint:
      run: npx oxlint {staged_files}
      glob: "*.{js,ts,svelte}"
    format:
      run: npx oxfmt --check {staged_files}
    typecheck:
      run: npm run check

pre-push:
  commands:
    test:
      run: npm run test
